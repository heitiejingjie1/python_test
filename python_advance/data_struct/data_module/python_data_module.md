## 特殊方法
    带有双下划线的方法
    例如:
        collection.len() == len(collection)
## 想让对象支持以下基本的语言结构并与其交互，需要实现特殊方法
  - 容器
  - 属性存取
  - 迭代(包括异步迭代**async for**)
  - 运算符重载
  - 函数和方法调用 
  - 字符串表示形式和格式化
  - 使用**await**的异步编程 
  - 对象创建和析构 
  - 使用**with**和**async with**语句管理上下文
